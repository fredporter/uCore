[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ucode-workspace"
version = "0.2.0-dev"
description = "uCode workspace — unified uCore + runtimes"
requires-python = ">=3.12,<3.13"


